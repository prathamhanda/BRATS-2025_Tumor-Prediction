Cell 1 (code) # Environment Setup and Variable Initialization for Advanced Post-Processing import sys
Cell 2 (markdown) # Pediatric Brain Tumor Segmentation (BraTS PEDs) — Research-Grade Pipeline This notebook impleme...
Cell 3 (code) # Section 0 — Core Principles: reproducibility first from __future__ import annotations
Cell 4 (markdown) ## Section 1 — Dataset & directory assumptions This section validates:
Cell 5 (code) # Section 1A — GPU server compatibility + required library checks import importlib
Cell 6 (code) # Section 1B — Locate dataset on GPU server mount (preferred) or local workspace from pathlib imp...
Cell 7 (code) # Section 1B.1 — Inspect dataset directories (training/validation) to confirm BraTS naming from p...
Cell 8 (code) # Section 1C — Validate one training + one validation case: modalities + label set + alignment # ...
Cell 9 (markdown) ## Section 1E — Infer label mapping (diagnostic) Your server dataset contains label values not co...
Cell 10 (code) # Section 1E — Infer label mapping (diagnostic) # Goal: infer meaning of label 3 (and others) for...
Cell 11 (code) # Section 1D — Visualize: mid-slices + label overlay sanity (train + val) import matplotlib.pyplo...
Cell 12 (markdown) ## Section 2 — Preprocessing (CRITICAL PHASE) We implement the roadmap preprocessing steps **in o...
Cell 13 (code) # Section 2.1 — Orientation & spacing validation (sample case) import numpy as np
Cell 14 (code) # Section 2.2 — Skull stripping (mandatory): HD-BET if available, else conservative fallback impo...
Cell 15 (code) # Section 2.3 — Intensity normalization inside brain mask (per-modality, per-subject) import nump...
Cell 16 (markdown) ## Section 3 — nnU‑Net v2 Baseline (Setup) **Goal:** set up nnU‑Net v2 and convert the dataset to...
Cell 17 (code) # Section 3.1 — Install & validate nnU‑Net v2 (in-notebook) import os
Cell 18 (code) # Section 3.2 — Configure nnU‑Net v2 environment paths from pathlib import Path
Cell 19 (code) # Section 3.3 — Export BraTS PEDs dataset to nnU‑Net v2 raw format (with label remap) import os
Cell 20 (code) !pip install nibabel
Cell 21 (code) # Section 3.4 — Sanity-check nnU‑Net export from pathlib import Path
Cell 22 (code) # Section 3.6 — nnU‑Net v2 planning/preprocessing + training commands # NOTE: These steps are com...
Cell 23 (markdown) ## Section 3.7 — nnU‑Net v2 Planning/Preprocessing (Run) This will run nnU‑Net v2 dataset verific...
Cell 24 (code) # Section 3.7.0 — nnU‑Net label fix: make labels consecutive (ET 4 -> 3) # nnU‑Net v2 requires la...
Cell 25 (code) # Section 3.7.0b — Fix nnU‑Net raw images that are named `.nii.gz` but are not gzip # nnU-Net use...
Cell 26 (code) # Section 3.7.0c — Targeted fix for a SimpleITK-unreadable file # If the source file is corrupt (...
Cell 27 (code) # Section 3.7.1 — Run nnU-Net v2 planning + preprocessing import os
Cell 28 (markdown) ## Section 3.8 — nnU‑Net v2 Baseline Training (Start) We’ll start **fold 0** training for `3d_ful...
Cell 29 (code) # Speed tuning / diagnostics (run BEFORE launching a fold) import os
Cell 30 (code) # Section 3.8.1 — Launch nnU-Net training fold 0 in background (log to file) import os
Cell 31 (code) # Section 3.8.2 — Monitor training log (tail + status) import os
Cell 32 (code) # Section 3.8.3 — (Optional) Schedule folds 1–4 sequentially after fold 0 completes import os
Cell 33 (code) # Section 3.8.4 — Monitor scheduler log (tail + status) import time
Cell 34 (markdown) ## Section 4 — Inference + Export (after folds 0–4) At this point, folds **0–4** should be trained.
Cell 35 (code) # Section 4.1 — Remote CUDA gate + nnU‑Net paths (stop if not on GPU server) import os
Cell 36 (code) # Section 4.2 — Verify folds 0–4 are complete (checkpoint_final.pth) import os
Cell 37 (code) # Section 4.3 — (Optional) nnU‑Net best-config helper #
Cell 38 (code) # Section 4.5a1 — Configure prediction target for Dice # Predict on training images so labelsTr e...
Cell 39 (markdown) ## Section 4.5a — Dice on `imagesTr` (adds ground-truth metrics) This will run prediction on `ima...
Cell 40 (code) # Section 4.4 — Run ensemble prediction across folds (GPU) #
Cell 41 (code) # Section 4.4x — Show relevant nnUNetv2_predict flags (for memory/stability) import subprocess
Cell 42 (code) # Section 4.4y — GPU memory snapshot (why OOM?) import torch
Cell 43 (code) # Section 4.4y2 — nvidia-smi process query (sometimes better than table) import subprocess
Cell 44 (code) # Section 4.4z — Release cached VRAM in this kernel import gc
Cell 45 (code) # Section 4.4ac — PyTorch CUDA memory stats (kernel-held allocations?) import torch
Cell 46 (code) # Section 4.4aa — Process snapshot (find who holds VRAM) import subprocess
Cell 47 (code) # Section 4.4ab — PIDs of nnU-Net/python processes (minimal) import subprocess
Cell 48 (code) # Section 4.4b — Monitor prediction progress (log tail + file counts) import os
Cell 49 (code) # Section 4.4c — Quick status (minimal output) import os
Cell 50 (code) # Section 4.4c3 — Wait until prediction completes (blocking) import os
Cell 51 (code) # Section 4.4c1 — Deep status (what is the PID doing?) import os
Cell 52 (code) # Section 4.4c2 — Wait & re-check (up to 2 minutes) import os
Cell 53 (code) # Section 4.4d — Stop prediction job (if needed) import os
Cell 54 (code) # Section 4.5a2 — Label audit (detect ET label value) #
Cell 55 (code) # Section 4.5 — Quick WT/TC/ET Dice (only if ground truth exists) #
Cell 56 (code) # Section 4.5b — ET prevalence / empty-region sanity check from pathlib import Path
Cell 57 (code) # Section 4.5c — WT/TC/ET voxel-volume summary (GT vs prediction) from pathlib import Path
Cell 58 (markdown) ## Section 4.6 — Post-process predictions (recommended) This step is optional but often gives a “...
Cell 59 (code) # Section 4.6a — Run ADVANCED post-processing into a new directory from __future__ import annotat...
Cell 60 (code) # Section 4.7 — Package predictions as a submission zip import os
Cell 61 (code) # Evaluate Post-Processed Metrics (Dice & HD95) import numpy as np
Cell 62 (markdown) ## Section 5 — Final challenge submission (imagesTs) You just produced an `imagesTr` zip (great f...
Cell 63 (code) # Section 5.1 — Switch to imagesTs + recommended settings for final submission # This does NOT ru...
Cell 64 (code) # Section 5.2 — Validate latest submission zip against imagesTs inputs from __future__ import ann...
Cell 65 (markdown) ## Section 6 — Visual QC overlays (FLAIR / T1ce + prediction) This is the mandatory “visual sanit...
Cell 66 (code) # Section 6.1 — Configure QC + pick cases from __future__ import annotations
Cell 67 (code) # Section 6.2 — Render overlays (FLAIR + pred, T1ce + pred) from __future__ import annotations
Cell 68 (markdown) ## Section 7 — Export a comprehensive visualisations pack This section creates `/workspace/pediat...
Cell 69 (code) # Section 7.1 — Create /workspace/pediatric_tumor_data/visualisations structure from __future__ i...
Cell 70 (code) # Section 7.2 — Locate raw data + prediction folders (imagesTs/imagesTr, raw/post) from __future_...
Cell 71 (code) # Section 7.3 — Bulk export overlays + comparisons + summary plots from __future__ import annotat...
Cell 72 (code) # Section 7.4 — Build a simple HTML gallery for judges from __future__ import annotations
Cell 73 (code) # Section 7.5 — Backup /workspace/pediatric_tumor_data as an archive (for local download) from __...
Cell 74 (code) # Section 7.6 — Upload /workspace/pediatric_tumor_data to Google Drive (no local PC transfer) #
Cell 75 (code) # Section 7.6B — One-time rclone config for Google Drive (OAuth) #
Cell 76 (code) # Section 7.7 — Start remote upload (continues even if you disconnect) #
Cell 77 (code) # Section 7.8 — Monitor upload status (tail log + check PID) from __future__ import annotations
Cell 78 (markdown) # Section 8 — Model Comparison: Benchmarking nnU-Net v2 Against Alternatives Rationale: Show why ...
Cell 79 (code) # Section 8.0 — Dependencies (auto-install if missing) from __future__ import annotations
Cell 80 (code) # Section 8.1 — Setup: Define models to compare from __future__ import annotations
Cell 81 (code) # Section 8.2 — Locate Dataset + nnU-Net Results + Output Folders from __future__ import annotations
Cell 82 (code) # Section 8.3 — Metrics: Dice (WT/TC/ET) + HD95 (Hausdorff 95%) in mm from __future__ import anno...
Cell 83 (code) # Section 8.4 — Load nnU-Net validation predictions (robust: .npz softmax OR .nii.gz seg) and (if...
Cell 84 (code) # Section 8.5 — Load Ground Truth Labels (labelsTr) + voxel spacing (mm) from __future__ import a...
Cell 85 (code) # Section 8.6 — Optional baselines (MONAI) [not required for nnU-Net comparison] from __future__ ...
Cell 86 (code) # Section 8.7 — Evaluate nnU-Net 3d_fullres Baseline (Dice + HD95) print("="*60)
Cell 87 (code) # Section 8.8 — Guaranteed baselines (no training required) [Dice + HD95] from __future__ import ...
Cell 88 (code) # Section 8.8 — Evaluate nnU-Net v2 3d_lowres (if available) [Dice + HD95] from __future__ import...
Cell 89 (code) # Section 8.9 — Evaluate nnU-Net v2 2d (if available) [Dice + HD95] from __future__ import annota...
Cell 90 (code) # Section 8.10 — Model Comparison Summary Table (and save) [Dice + HD95] from __future__ import a...
Cell 91 (markdown) ### Montage output (overlay + tumor parameters) - The saved montage now renders **MRI anatomy (gr...
Cell 92 (code) # Section 8.11 — Visualisations: Dice + HD95 + sample slices from __future__ import annotations
Cell 93 (markdown) ## Section 9 — Batch validation export (max visualisations per case) > Goal: run (or load) nnU-Ne...
Cell 94 (code) # Section 9A — Inspect validation folder + GT availability (GPU server) from __future__ import an...
Cell 95 (code) # Section 9 — Batch validation export (max visualisations per case) from __future__ import annota...
Cell 96 (markdown) ## Section 9B — Labeled export (imagesTr/labelsTr with GT) If your official validation/test split...
Cell 97 (code) # Section 9B — Labeled export (imagesTr/labelsTr with GT) from __future__ import annotations
Cell 98 (code) # Section 9B — Quick output locator (no terminal) from pathlib import Path
Cell 99 (code) # Section 9C — Summarize per_case_results.csv (GT availability + aggregate metrics) from pathlib ...
Cell 100 (markdown) ## Section 9D — Export STL meshes from predicted masks (WT/TC/ET) This section converts 3D segmen...
Cell 101 (code) !pip install scikit-image
Cell 102 (code) # Section 9D — STL export (marching cubes) from __future__ import annotations
Cell 103 (markdown) ## Section 9E — Pick 1–2 “showcase” cases + export a handoff bundle (for 3D printing) **Goal (non...
Cell 104 (code) # Section 9E — Case selection + handoff bundle export from __future__ import annotations
Cell 105 (code) # Section 9E-B — Export STL meshes for the exported showcase cases (pred + optional GT) from __fu...
Cell 106 (code) # Section 9E-C — Backfill: copy original BraTS modality NIfTIs into existing showcase bundles fro...
Cell 107 (code) # Section 9E-D — Update README with modality mapping (from dataset.json) from __future__ import a...
Cell 108 (markdown) ## Section 5 — Alternative Pipeline: MONAI SwinUNETR This section introduces a standalone PyTorch...
Cell 109 (code) # Install all required packages on remote GPU %pip install -q torch monai nibabel "numpy<2.0" einops
Cell 110 (code) # Define the standalone dataset construction and pipeline components. # We map nnU-Net dataset st...
Cell 111 (code) # Section 5.2 — Custom Loss Function: Dice + Focal Tversky Loss # This module is reusable and com...
Cell 112 (markdown) ### Section 5.3 — Strict MONAI DataLoader Implementation Directly reusing the pre-existing nnU-Ne...
Cell 113 (code) import sys from pathlib import Path
Cell 114 (code) # 6. Define Validation Dataset and DataLoader # Strictly targets imagesTs. No random cropping. No...
Cell 115 (code) batch = next(iter(train_loader)) print("Image shape:", batch["image"].shape)
Cell 116 (code) %%writefile train_swin.py import os
Cell 117 (code) import subprocess subprocess.Popen(
Cell 118 (code) !pkill -f train_swin.py
Cell 119 (code) !ps aux | grep train_swin.py
Cell 120 (code) import sys print(sys.executable)
Cell 121 (code) import subprocess, sys subprocess.Popen(
Cell 122 (markdown) ### Monitoring Cells Run the following cells in a separate window or after the training finishes ...
Cell 123 (code) 
Cell 124 (code) # View the last 50 lines of the training log !tail -n 50 training_bg.log
Cell 125 (code) !pkill -f train_swin.py
Cell 126 (code) !ls -l /workspace/checkpoints
Cell 127 (code) # Live view: Run this safely if you export this workflow or open a new console. # Be careful: run...
Cell 128 (code) import inspect from monai.networks.nets import SwinUNETR
Cell 129 (code) import os from pathlib import Path
Cell 130 (code) # Calculate B & A (Before and After) Option B locally import sys
Cell 131 (code) import matplotlib.pyplot as plt import matplotlib.patches as patches
Cell 132 (code) print("✅ Visualization generated successfully!")
Cell 133 (code) # Soft Ensemble import torch
Cell 134 (code) 
Cell 135 (code) import torch import torch.nn.functional as F
Cell 136 (code) import pandas as pd import torch